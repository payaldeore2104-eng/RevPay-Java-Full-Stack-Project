# Server
server.port=8080

# Oracle DB Connection (Simulated/Placeholder)
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=revpay_user
spring.datasource.password=revpay123
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging
logging.level.com.revpay=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO

# Multipart File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=12MB

# Upload storage directory (relative to working directory)
app.upload.dir=uploads/verification-docs

# ── Gmail SMTP (for OTP Email) ──────────────────────────────────────────────
# IMPORTANT: Replace with your Gmail address and App Password
# How to get App Password:
#   1. Go to Google Account > Security
#   2. Enable 2-Step Verification
#   3. Search "App Passwords" → Select Mail → Generate
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=payaldeore2108@gmail.com
spring.mail.password=xhne ekwh xvim nipv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000
